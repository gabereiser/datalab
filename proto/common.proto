syntax = "proto3";
option go_package="github.com/gabereiser/datalab/rpc/common";
package datalab;

message IDResponse {
    bool success = 1;
    repeated string errors = 2;
    optional string id = 3;
}

message Response {
    bool success = 1;
    repeated string errors = 2;
}